/**
 * Point d'entrée pour les scopes
 * 
 * Ce fichier réexporte les classes et interfaces de scopes pour maintenir
 * la compatibilité avec les imports existants.
 */

export { GlobalScope, GlobalScopeCallback, ScopeManager, DynamicScope, DynamicScopeCallback } from './scopes/index';

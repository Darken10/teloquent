/**
 * Types utilisés dans l'ORM Teloquent
 * Ce fichier contient les définitions de types pour éviter les erreurs de compilation
 */

// Types Knex pour contourner les problèmes d'importation
export type KnexQuery = any;
export type KnexRaw = any;
export type KnexSchema = any;
